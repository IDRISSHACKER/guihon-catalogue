---
- name: Get username
  become: false
  shell:
    cmd: whoami
  register: user

- name: Set home username
  set_fact:
    username: "{{ user.stdout }}"

- name: Ecucute test unit
  shell:
    chdir: "/home/{{ username }}/{{  app_destination  }}"
    cmd: "npm run test"
...
