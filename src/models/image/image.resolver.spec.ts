import { ImageResolver } from './image.resolver';

describe('ImageController', () => {
  let controller = ImageResolver;


  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
