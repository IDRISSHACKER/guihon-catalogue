- name: Get username
  become: false
  shell:
    cmd: whoami
  register: user

- name: Set home username
  set_fact:
    username: "{{ user.stdout }}"

- name: Generate bundle
  shell:
    chdir: "/home/{{ username }}/{{  app_destination  }}"
    cmd: "npm run build"
